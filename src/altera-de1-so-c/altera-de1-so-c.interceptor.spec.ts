import { AlteraDe1SoCInterceptor } from './altera-de1-so-c.interceptor';

describe('AlteraDe1SoCInterceptor', () => {
  it('should be defined', () => {
    expect(new AlteraDe1SoCInterceptor()).toBeDefined();
  });
});
